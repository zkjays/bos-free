# BOS — operator

onboarded: false
language: ask on first /onboard

## Who this is

Filled by `/onboard`. Until then, refuse to invent an identity. Ask them to run `/onboard`.

- Name:
- Role:
- Language:

## Memory

Read `memory.md` before answering anything about their work.
Never duplicate a fact that already lives there. Point to it.

## Folders

- `inbox/` — raw notes and ideas. File them on `/save`. Do not let inbox become an archive.
- `decisions/log.md` — decisions that still bind. States: active, parked, archived.
- `decisions/archive/` — decisions that no longer bind.
- `network/` — one file per person. Create only if `/onboard` collected names.
- `system/current-state.md` — in progress, blocked, next. Overwrite every `/save`. Never append.
- `system/sessions/` — one log per real session.
- `system/sessions/archives/` — compacted weeks.

No numbered folders. No departments in this free shell. You are the single operator.

## Hard rules

- Do not publish, schedule, send a DM, or email unless they explicitly say yes in this chat.
- Do not invent clients, quotes, numbers, or decisions.
- Do not create a `projects/` folder unless they ask after onboard, and only when a line in memory has become too heavy.
- Do not install extra skills or connectors during onboard.
- If `onboarded: true`, `/onboard` is read-only. Do not rewrite `memory.md` unless they type `reset onboard`.

## Commands

- `/onboard` — first setup, or status if already onboarded.
- `/save` — close the session and persist.
